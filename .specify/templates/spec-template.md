# 功能规格：[功能名称]

**功能分支**：`[###-feature-name]`

**创建日期**：[DATE]

**状态**：草稿

**输入**：用户描述："$ARGUMENTS"

## 用户场景与测试 *(必填)*

<!--
  重要：用户故事必须按优先级排列，优先级表示用户旅程的重要性。
  每个用户故事都必须可以独立测试：也就是说，只实现其中一个故事时，
  仍然可以交付一个有价值的最小可用版本。

  使用 P1、P2、P3 等优先级标记用户故事，P1 代表最关键的故事。
  每个故事都应该是一个可独立开发、测试、部署和演示的功能切片。
-->

### 用户故事 1 - [简短标题]（优先级：P1）

[用普通业务语言描述这个用户旅程]

**优先级原因**：[说明它为什么有这个优先级，以及它提供的核心价值]

**独立测试**：[说明如何独立验证这个故事，例如“通过[具体操作]即可完整测试，并交付[具体价值]”]

**验收场景**：

1. **假设** [初始状态]，**当** [用户动作]，**那么** [预期结果]
2. **假设** [初始状态]，**当** [用户动作]，**那么** [预期结果]

---

### 用户故事 2 - [简短标题]（优先级：P2）

[用普通业务语言描述这个用户旅程]

**优先级原因**：[说明它为什么有这个优先级，以及它提供的核心价值]

**独立测试**：[说明如何独立验证这个故事]

**验收场景**：

1. **假设** [初始状态]，**当** [用户动作]，**那么** [预期结果]

---

### 用户故事 3 - [简短标题]（优先级：P3）

[用普通业务语言描述这个用户旅程]

**优先级原因**：[说明它为什么有这个优先级，以及它提供的核心价值]

**独立测试**：[说明如何独立验证这个故事]

**验收场景**：

1. **假设** [初始状态]，**当** [用户动作]，**那么** [预期结果]

---

[按需增加更多用户故事，每个故事都必须有明确优先级]

### 边界情况

<!--
  待填写：把本功能需要覆盖的边界条件和异常场景替换到下面列表中。
-->

- 当 [边界条件] 发生时，系统如何处理？
- 系统如何处理 [错误场景]？
- 网关如何处理上游服务超时、限流或返回格式异常？
- 网关如何避免服务商密钥、访问令牌和敏感提示词/图片内容暴露给客户端或日志？
- 网关如何在处理大图片或 multipart 负载时避免无界内存占用？

## 需求 *(必填)*

<!--
  待填写：把下面的示例替换为真实、可测试、无歧义的功能需求。
-->

### 功能需求

- **FR-001**：系统必须 [具体能力，例如“允许用户创建账号”]
- **FR-002**：系统必须 [具体能力，例如“校验邮箱格式”]
- **FR-003**：用户必须能够 [关键交互，例如“重置密码”]
- **FR-004**：系统必须 [数据要求，例如“保存用户偏好设置”]
- **FR-005**：系统必须 [行为要求，例如“记录所有安全事件”]
- **FR-006**：系统必须为每个公开网关路由定义契约，包括请求结构、响应结构、调用方认证要求、超时行为和上游错误映射。
- **FR-007**：系统必须把服务商凭证和上游账户标识保留在服务端。
- **FR-008**：系统必须为每个新增或变更的外部可见网关行为识别所需的契约测试或集成验证。

*不明确需求的标记示例：*

- **FR-009**：系统必须通过 [需要澄清：调用方认证方式未确定，是 API Key、OAuth2 还是 mTLS？] 认证调用方。
- **FR-010**：系统必须保留请求元数据 [需要澄清：保留周期未确定]。

### 关键实体 *(涉及数据时填写)*

- **[实体 1]**：[它代表什么，关键属性是什么，不写实现细节]
- **[实体 2]**：[它代表什么，以及它和其他实体的关系]

## 成功标准 *(必填)*

<!--
  待填写：定义可衡量的成功标准。
  成功标准必须与技术实现无关，并且可以被验证。
-->

### 可衡量结果

- **SC-001**：[可衡量指标，例如“用户可以在 2 分钟内完成账号创建”]
- **SC-002**：[可衡量指标，例如“系统可以支持 1000 名并发用户且无明显体验下降”]
- **SC-003**：[用户满意度指标，例如“90% 的用户可以首次尝试即完成主要任务”]
- **SC-004**：[业务指标，例如“将与 [问题] 相关的支持工单减少 50%”]

## 假设

<!--
  待填写：根据需求中未明确说明的部分，记录合理默认假设。
-->

- [关于目标用户的假设，例如“用户拥有稳定的网络连接”]
- [关于范围边界的假设，例如“移动端原生 App 不包含在 v1 范围内”]
- [关于数据或环境的假设，例如“复用现有认证系统”]
- [关于外部依赖的假设，例如“需要访问现有用户资料服务”]

## 宪章对齐 *(必填)*

- **Spring AI 优先**：[确认 Spring AI 集成路径和模型配置，包括是否默认使用 `gpt-image-2`]
- **WebFlux 透传网关**：[确认响应式请求/响应流程，以及任何经过说明的非透传行为]
- **边界验证**：[列出路由、上游失败、认证、错误映射、流式响应或大负载行为所需的契约/集成检查]
- **密钥与可观测性**：[说明服务端凭证处理、日志脱敏、关联 ID 和可操作错误分类]
