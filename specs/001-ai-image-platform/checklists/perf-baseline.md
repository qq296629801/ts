# 性能基线（001-ai-image-platform）

**日期**：2026-05-27 | **状态**：待压测填写实测值

## 规格目标（FR-034–036）

| 指标 | 目标 | 测量方法 | 实测（待填） |
|------|------|----------|--------------|
| 常规 API P95 | ≤ 1s（FR-034） | JMeter/Gatling：登录、广场列表、配额、消息第一页 | — |
| 生图端到端 P95 | ≤ 15s（FR-035） | 网关 `POST /api/v1/ai/image/generate` 含上游 | — |
| 并发活跃用户 | ≥ 1000（FR-036） | 混合场景压测（浏览+登录+对话+下单） | — |

## 建议压测场景

1. **只读**：`GET /api/v1/templates`、`GET /api/v1/user/quota`（100 并发，5 分钟）
2. **写路径**：注册+登录（20 并发）
3. **生图**：10 并发（受上游 OpenAI 配额限制，单独报表）
4. **支付**：创建订单+查询（50 并发）

## 通过标准

- P95 达到上表目标，或记录偏差原因（上游 SLA、网络、实例规格）
- 生图 409 单路并发策略在压测中无超扣次数

## 环境记录

| 项 | 值 |
|----|-----|
| 实例 | （待填） |
| platform-api 副本 | 1 |
| gateway-service 副本 | 1 |
| MySQL/Redis | docker-compose.dev |
